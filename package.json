{
  "name": "ai-chat-pdf-exporter",
  "version": "1.2.0",
  "description": "Export conversations from Claude, ChatGPT, Gemini, Perplexity, Grok, and Rovo into PDF documents.",
  "main": "content.js",
  "type": "commonjs",
  "scripts": {
    "test": "jest --colors",
    "test:coverage": "jest --coverage",
    "prebuild": "npm test",
    "build": "node scripts/build.js",
    "add-test": "node scripts/add-test.js"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
